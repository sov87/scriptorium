# Handoff Instructions (New Chat Start)

Paste this into a new chat:

“Read docs/PROJECT_CONTEXT.md and docs/DECISIONS.md in the repo. Treat them as the source of truth. Do not propose changes that violate Non-negotiables or Design Decisions. Start by listing the top 3 next actions from ‘Next work (priority order)’ and provide exact file/command-level steps. If you need repo files, ask for them explicitly.”

Operational rule:
- If context gets long: update PROJECT_CONTEXT.md + DECISIONS.md, commit, then start fresh.
